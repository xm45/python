download the photo from pixiv

Simulate the HTTP request

can only download the photo which you can view with account

use `requests` module, see http://www.python-requests.org for more

4 thread for downloading, to avoid the request detection

by Meepo